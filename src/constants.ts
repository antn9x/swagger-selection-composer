import { Method } from "./schema.tpl";

export const methods: Method[] = ['get', 'post', 'put', 'delete'];

export const confirm = ["Yes", "No"];

export const PACKAGE_NAME = 'swagger-api-generator';
